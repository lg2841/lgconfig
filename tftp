service tftp
{
disable = no
port = 69
socket_type = dgram
protocol = udp
wait = yes
user = root
server = /usr/sbin/in.tftpd
server_args = /home/lg2841/work/tbea/rootfs/boot
per_source = 11
cps = 100 2
flags = IPv4
}
